# 16 周职业转型路线

这不是一份面向学生的课程表，而是一条面向在职数据开发、数仓建模工程师和 SQL 重度使用者的
Data + AI 职业转型路线。默认每周投入 8～10 小时，在不脱离日常工作的前提下，持续建设同一个
Mini-LLM Data Factory。

16 周指 16 个能力阶段。完成第 4 周后，可以插入一次 8～10 小时的 Bridge 01 源码强化周；选择该路径时，
总日历约为 17 周，但后续能力阶段和验收门槛不变。

路线不以“看完多少论文”或“学会多少新框架”为完成标准。16 周结束时，你需要能够独立交付、解释和复现：

```text
原始文档
→ 可治理的数据版本
→ Token / Packed Sequence / Shard
→ Tiny LLM 训练
→ 固定评测
→ 数据归因与下一版改进
```

## 转型目标

传统数据工程的核心交付物通常是表、任务、指标和 SLA。本路线把责任边界向模型侧推进：

| 原有能力 | 转型后的责任 |
| --- | --- |
| SQL、ETL、数仓分层 | 构造可训练的 Document、Sample、Token 和 Sequence |
| 主键、空值、Schema 质量 | 处理语义质量、近似重复、评测污染、PII 和许可 |
| 表级和字段级血缘 | 建立来源文档到 Token、训练运行和模型指标的血缘 |
| 分区、Shuffle、调度 | 设计训练 Shard、样本顺序和分布式加工 Pipeline |
| 任务成功率和时效 | 同时关注有效 Token、DataLoader Wait、Tokens/s 和恢复时间 |
| 报表验收 | 用固定预算训练和评测验证数据版本 |

完成路线不代表成为算法研究员、CUDA 工程师或分布式存储内核专家。目标是成为能够连接数据、计算、
存储、训练和评测的 AI 数据工程师，并能根据真实瓶颈选择后续纵深。

## 为什么采用螺旋式路线

纯线性路线通常先讲几个月数据处理，再到最后才训练模型。这样容易把 AI 数据工程重新学成一套新的 ETL，
也无法及时验证去重、过滤或混合是否真的有价值。

本路线先建立一条很小但完整的反馈链，然后重复四次：

1. **语义闭环**：文档如何变成模型消费的 Token；
2. **质量闭环**：数据变化是否影响固定评测；
3. **规模闭环**：数据量和分布变化如何影响加工与供给吞吐；
4. **系统闭环**：训练中断、I/O 和存储架构如何影响成本与可靠性。

每一圈只增加一种主要复杂度，保留上一圈的可复现基线。

## 主线与进阶支线

### 必修主线

所有参与者都应完成：

- Document → Token → Sequence → Loss；
- 数据质量、近似去重、污染检测和数据混合；
- Dataset Manifest、Run Manifest 和样本血缘；
- Tiny LLM 基线与受控数据 A/B；
- DuckDB/Parquet 数据加工和 DataLoader 瓶颈诊断；
- Checkpoint 恢复；
- smallpond 与 3FS 的架构取舍；
- 一份可复现的毕业报告。

### Infra 进阶支线

有 Linux、GPU 或集群条件时再完成：

- Ray 或 smallpond 多 Worker 执行；
- 多 GPU DDP 与通信分析；
- MinIO、NFS 或其他共享存储对比；
- 3FS 测试集群、NVMe、NUMA 和 RDMA 实验；
- smallpond 优化器、Planner 或 3FS 源码深读。

进阶支线不能替代必修主线。没有数据—模型反馈证据时，集群规模本身不构成毕业成果。

## 每周执行合同

建议把每周 8～10 小时固定分配为：

- 1～2 小时：带着问题阅读论文、官方文档或源码；
- 4～5 小时：完成本地代码和实验；
- 1～2 小时：分析指标、失败样例和证据边界；
- 1 小时：整理可公开的实验记录和职业叙事。

每周必须留下六类证据：

1. **问题**：这周解决哪个真实的数据或训练问题？
2. **输入**：数据、配置、版本和前置条件是什么？
3. **实现**：代码和命令如何复现？
4. **指标**：结果如何度量？
5. **边界**：结果不能证明什么？
6. **迁移**：它和已有 SQL、数仓或数据平台经验如何连接？

实验不预设“正确方向”。例如，去重后验证 Loss 可能改善、无显著变化或退化；晋级标准是变量受控、结果可复现、
结论有证据，而不是得到预先期待的数字。

## 第一阶段：接通数据到模型的最小闭环

### 第 1 周：重新定义数据工程交付物

**迁移锚点**：从表、分区和指标，迁移到 Document、Sample、Token、Sequence、Batch 和 Loss。

- 完成 Lab 00 环境检查；
- 选择一个熟悉的数据任务，画出它与训练数据链路的概念映射；
- 理解 Next Token Prediction、标签右移和 `loss_mask`；
- 阅读 DeepSeek 技术栈全景，不进入实现细节。

**产出**：环境报告、个人能力基线、文档到 Loss 的链路图。

**晋级门槛**：能够解释模型消费的是有顺序和边界的 Token Sequence，而不是普通数据表中的一行。

### 第 2 周：从文档到训练 Token

**实验**：完成 Lab 01 的真实本地 Pipeline。

- Unicode 和空白规范化；
- 内容哈希与精确去重；
- Byte-level BPE Tokenizer；
- BOS/EOS、Document Packing、固定长度 Sequence；
- Dataset Manifest 和 Token 级来源血缘。

**指标**：文档数、重复率、Token 数、Packing 利用率、Pipeline Fingerprint。

**产出**：Tokenizer、Sequence、Manifest 和可重复运行记录。

**晋级门槛**：同一输入与配置生成相同 Fingerprint，并能从一个 Sequence 追溯到来源文档。

### 第 3 周：Tiny LLM 基线

**实验**：使用 Lab 01 的 Sequence 训练一个最小 Transformer，不追求生成能力或参数规模。

- 建立 Dataset、DataLoader 和最小训练循环；
- 记录训练/验证 Loss、Tokens/s、随机种子和数据版本；
- 保存并重新加载最小 Checkpoint；
- 建立计划中的 Run Manifest，使训练运行引用 Dataset Manifest。

**产出**：Loss 曲线、训练日志、Checkpoint、Run Manifest。

**晋级门槛**：训练稳定、Loss 计算正确、运行可以复现；不以“生成文本像人”作为本阶段标准。

## 第二阶段：证明数据变化能够被模型看见

### 第 4 周：第一次受控数据 A/B

**实验**：构造基线数据与受控污染数据，只改变一个数据变量。

- 固定 Tokenizer、模型结构、初始化、训练 Token 预算和验证集；
- 注入重复、乱码或无意义片段中的一种；
- 比较训练动态、固定验证 Loss、有效 Token 和吞吐；
- 记录无显著差异和反常结果，不为了“证明数据重要”挑选结果。

**产出**：第一份数据版本 A/B 报告。

**晋级门槛**：能够区分实验变量、控制变量和混杂因素，并把结论限定在当前数据规模与训练预算内。

### Bridge 01：DeepSeek 源码与论文强化周

**定位**：在进入具体质量算法前，把已经运行的本地链路映射到 DeepSeek-V3、smallpond 和 3FS 的官方证据。

- 从 DeepSeek-V3 的数据构造与训练并行反推数据系统工作负载；
- 追踪 smallpond 的 `DataFrame → Node → Optimizer → Planner → Task`；
- 追踪 3FS 的 Metadata Service、FoundationDB、Storage Service、CRAQ 和 Client；
- 用 Dataset、Run、Experiment Manifest 完成本地责任映射；
- 做一次 Go / No-Go 架构评审，区分源码理解、本地实验和集群证据。

**产出**：工作负载卡、smallpond 调用链、3FS 元数据/数据路径图、本地架构评审。

**晋级门槛**：闭卷验收达到 16/20，且没有把官方集群数字、源码理解或架构推演冒充成本地验证。

完整步骤见 [Bridge 01 教程](docs/tutorials/bridge-01-deepseek-source-study.md)。

### 第 5 周：近似去重与评测污染

**迁移锚点**：从数据库唯一键迁移到内容相似性和训练/评测集合隔离。

- 实现 Shingling、MinHash/LSH 或等价的近似去重方案；
- 建立人工标注的小规模真值集；
- 识别文档级、段落级和跨数据集重复；
- 检查训练集与固定评测集污染。

**指标**：重复率、Precision、Recall、误判/漏判样例、处理吞吐。

**产出**：重复簇、保留策略、污染报告和失败样例。

**晋级门槛**：不能只报告去重率；必须解释阈值取舍和误判成本。

### 第 6 周：质量、治理、混合与 Sharding

**迁移锚点**：从规则质量和数仓分区迁移到可训练性、领域配比与训练 Shard。

- 建立结构、语言、内容、安全、PII 和许可的质量维度；
- 区分确定性规则、分类器和 LLM Judge 的证据等级；
- 按语言、领域、来源或难度进行确定性混合；
- 生成大小受控、分布可解释的训练 Shard；
- 重新运行第 4 周 A/B，验证质量策略的影响。

**指标**：过滤原因分布、有效 Token 产出率、领域分布、Shard 倾斜、血缘覆盖率。

**产出**：质量报告、Dataset Card、Manifest v2、训练 Shard。

**晋级门槛**：任一训练样本都能追溯到来源、规则版本、Tokenizer、混合权重和 Shard。

## 第三阶段：把正确的数据加工扩展到更大规模

### 第 7 周：DuckDB、Arrow 与 Parquet 基线

**迁移锚点**：从 SQL 执行计划和分区裁剪迁移到本地向量化数据加工。

- 用 DuckDB/Parquet 重构一段已有数据 Pipeline；
- 观察列式存储、Predicate Pushdown 和中间物化；
- 对比 Python 逐行处理与 DuckDB 的执行时间和内存；
- 明确 Arrow 零拷贝成立与不成立的边界。

**产出**：同一工作负载的实现与基准报告。

**晋级门槛**：能够用 Profile 解释性能差异，而不是只报告最终耗时。

### 第 8 周：smallpond、Partition 与 Shuffle

**实验**：用 smallpond 或其最小可运行路径完成一次多阶段数据加工。

- 阅读 `dataframe.py` 和逻辑节点的关键路径；
- 理解 Lazy DAG、Hash Partition、Shuffle 和 Join；
- 对比“强单机引擎 + 简单分布式编排”与传统 Spark 思路；
- 保留逻辑计划、物理任务和中间文件证据。

**产出**：smallpond Pipeline、执行图和架构取舍笔记。

**晋级门槛**：能够解释为什么 Join 两侧需要兼容分区，以及共享存储如何改变 Shuffle 状态管理。

### 第 9 周：倾斜、小文件与失败恢复

- 人工制造热点 Key、分区不均和小文件问题；
- 比较 Repartition、Key 拆分、文件合并等最小修复；
- 注入 Worker 或任务失败，观察重试与中间结果复用；
- 根据真实证据决定是否继续阅读 Optimizer、Planner 和执行层源码。

**指标**：分区大小分布、P50/P99 任务时间、重试次数、总执行时间和资源峰值。

**产出**：故障实验记录和性能诊断报告。

**晋级门槛**：先定位瓶颈，再做一个最小改动，并针对原问题复测。

### 第 10 周：DataLoader 与数据供给

**迁移锚点**：从数据产出时效迁移到 GPU/训练循环能否持续获得有效 Token。

- 比较 Map-style 与 Iterable-style Dataset；
- 调整 Worker、预取、缓存、Shard 大小和样本顺序；
- 区分存储、解压、Tokenization 和 Batch 组装耗时；
- 在没有 GPU 时使用训练循环空转时间和 CPU Tokens/s 建立本地证据。

**指标**：DataLoader Wait、Samples/s、Tokens/s、CPU/内存、随机读延迟。

**产出**：DataLoader Profile 和瓶颈决策树。

**晋级门槛**：能够用指标判断问题属于数据格式、文件组织、CPU 处理还是存储。

## 第四阶段：理解训练可靠性和 AI 存储

### 第 11 周：Checkpoint 与精确恢复

- 保存模型、优化器、调度器、随机状态和数据游标；
- 在训练中途注入中断；
- 恢复后比较 Loss 连续性和样本顺序；
- 建立 Dataset Version、Run Version 和 Checkpoint 的关联。

**指标**：Checkpoint 大小、写入时间、恢复时间、Loss 差异、样本重复/遗漏。

**产出**：可恢复训练实验与恢复证据。

**晋级门槛**：恢复不是“能继续跑”，而是能够解释数据顺序和训练状态是否一致。

### 第 12 周：分布式训练的数据视角

- 理解 Global Batch、Micro Batch 和梯度累积；
- 建立 DP、TP、PP、EP、ZeRO 与数据分发/通信的关系；
- 理解 MoE All-to-All 对网络和批次组织的影响；
- 有两张 GPU 时完成最小 DDP；没有时完成可验证的设计推演。

**产出**：并行策略—数据供给—通信关系图；可选 DDP 实验。

**晋级门槛**：能够解释不同并行策略如何改变数据、网络和 Checkpoint 压力，而不是背诵缩写。

### 第 13 周：把存储看成训练数据平面

- 描述顺序扫描、随机样本读、Shuffle 中间文件和并行 Checkpoint 四类负载；
- 对比本地 SSD 与一种可用的远程/共享存储；
- 测量随机读延迟、吞吐、并发和 Checkpoint 时间；
- 区分文件系统、对象存储、湖仓表格式和 Catalog 的职责。

**产出**：I/O 工作负载模型和本地存储基线。

**晋级门槛**：先证明存在存储瓶颈，再讨论替换存储架构。

### 第 14 周：3FS 架构与证据边界

- 阅读 Manager、Metadata、Storage、Client 的职责；
- 理解 FoundationDB 元数据事务、Chunk、Stripe、Chain 和 CRAQ；
- 推演文件创建、写入、读取、故障切换和 Checkpoint 路径；
- 对比 3FS 与当前团队存储架构的适用条件；
- 有合适 Linux 集群时再进入部署和 RDMA/NVMe 实验。

**产出**：3FS 架构评审、工作负载适配矩阵和可选集群报告。

**晋级门槛**：本地设计推演、官方集群基准和自己运行的集群结果必须分开标注。

## 第五阶段：形成作品、方法和职业证据

### 第 15 周：正式数据版本 A/B 与失败归因

- 选择去重、质量、混合、Packing 或 Sharding 中的一个数据变量；
- 固定模型、Tokenizer、初始化、训练 Token 预算和评测集；
- 同时比较数据有效性、系统效率和模型结果；
- 从失败评测样例追溯到来源文档、处理规则和训练版本；
- 提出下一版数据改进，但不在本周同时改变多个变量。

**产出**：正式 A/B 报告、失败样例归因和下一版假设。

**晋级门槛**：结论能够回到具体数据变化，并主动报告反例、方差和不确定性。

### 第 16 周：开源毕业项目与独立复现

- 整理端到端 Mini-LLM Data Factory；
- 补齐 Tutorial、How-to、Reference 和 Explanation；
- 提供一条从环境到评测的可复现命令；
- 清理密钥、内部路径和许可不明数据；
- 邀请另一位在职数据工程师只依赖仓库文档完成复现；
- 根据真实复现问题修正文档和工程入口。

**毕业交付物**：

1. 可版本化的数据 Pipeline 和 Dataset Manifest；
2. Tiny LLM 训练、Run Manifest 和 Checkpoint；
3. 数据质量、计算或 I/O 的性能诊断；
4. 一次受控数据版本 A/B 与失败归因；
5. smallpond/3FS 的源码或架构阅读证据；
6. 一篇区分本地、集群和生产证据的技术报告；
7. 他人独立复现记录。

毕业标准不是“16 周打卡完成”，而是新参与者能复现实验，你也能在面试、技术评审或真实工作中解释每个取舍。

## 论文与源码如何进入路线

论文和源码不是独立阅读任务，而是实验的设计输入。每次只提交一页阅读卡：

```text
问题：本周实验要回答什么？
机制：论文或源码采用了什么设计？
约束：这个设计依赖哪些规模、硬件或工作负载？
实验：本地能够验证哪一部分？
边界：哪些结论只能引用，不能声称自己验证过？
迁移：它与传统数仓/数据平台的哪个概念相连？
```

默认阅读入口见 [DeepSeek 源码与论文阅读索引](docs/reference/reading-list.md)。

## 加速与延长

- 已有机器学习基础且每周可投入 15 小时以上，可以合并相邻两周形成 8 周强化路线；
- 工作繁忙时可以按阶段延长到 24 周，但不要跳过 Tiny LLM、受控 A/B、数据血缘和复现；
- 对 AI Infra 感兴趣时，在完成必修主线后继续深入 smallpond Planner、分布式训练或 3FS；
- 对训练数据治理感兴趣时，继续深入合成数据、SFT/偏好数据、许可与删除传播。

路线允许变慢，但不允许用阅读笔记替代实验，也不允许用本地小数据结论冒充生产验证。
