# 贡献指南

感谢你帮助更多数据工程师进入 AI 数据工程领域。本项目优先接受可复现的实验、准确的原理说明和明确的失败证据。

## 开发环境

```bash
make setup
make validate
```

也可以不使用 Make：

```bash
uv sync --python 3.11 --extra dev
uv run ruff check .
uv run pytest
```

## 文档类型

新增文档前先选择唯一主要类型：

- `docs/tutorials/`：带领学习者完成一个结果；
- `docs/how-to/`：解决一个已知问题；
- `docs/reference/`：描述接口、契约或术语；
- `docs/explanation/`：解释原理、关系和取舍。

不要在一篇文档里同时承担教程、API 字典和架构论文三个职责。

## 实验的完成定义

一个实验只有同时满足以下条件才能标记“可运行”：

1. 明确输入、输出、前置条件和执行命令；
2. 固定依赖、配置和随机性；
3. 产出机器可读的 Manifest 或 Metrics；
4. 至少包含一个正常测试和一个失败测试；
5. 区分本地实验、集群基准和生产证据；
6. 说明结果不能证明什么。

## 代码要求

- Python 代码使用 UTF-8，无 BOM；
- 对外接口提供类型标注；
- 关键意图使用简洁中文注释；
- 通过 `ruff` 和 `pytest`；
- 不在多个模块重复维护数据契约或指标口径。

## 数据与安全

- 不提交密钥、Token、内部路径和个人数据；
- 不提交来源或许可不明确的数据集；
- 示例数据应当是自行编写、公共领域或明确允许再分发的内容；
- 报告失败日志时先清理凭据和个人信息。

## 引用与证据

- 优先引用项目官方仓库、官方文档和原始论文；
- 基准数据必须注明硬件、软件版本、数据规模和工作负载；
- 不把 README 中的性能数字改写成所有场景通用的结论；
- 未运行过的操作应标注为设计分析或待验证。

## 提交流程

1. 创建一个聚焦单一主题的分支；
2. 完成代码、文档和测试；
3. 运行 `make validate`；
4. 在 Pull Request 中提供命令、结果和证据等级；
5. 不顺带重构与本次目标无关的模块。
