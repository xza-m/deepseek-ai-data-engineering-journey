# Lab 07：DataLoader 与 I/O

状态：`可运行`

```bash
make lab07
```

输入：Lab 05 Training Shard，并引用 Lab 06 Compute Fingerprint。

输出：`dataloader_report.json` 与 `dataloader_decision.md`。

验收：Map/Iterable Profile 的 Sample、有效 Token 和唯一 Sequence 完整一致；性能数字只作为当前机器观测。
